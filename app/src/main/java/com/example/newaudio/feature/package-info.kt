/**
 * Feature layer — UI components and presentation logic.
 *
 * This layer contains:
 * - ViewModels that manage UI state and coordinate use cases
 * - Composable UI components and screens
 * - UI models and presentation-specific data structures
 * - Navigation and user interaction handling
 *
 * Import rules:
 * - Can import from domain/ and util/ packages
 * - Can import data/ only for testing (fake implementations)
 * - Contains Android UI framework dependencies (Compose, Activities, etc.)
 */
package com.example.newaudio.feature